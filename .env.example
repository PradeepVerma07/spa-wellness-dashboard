ADMIN_SESSION_SECRET=replace-with-a-long-random-secret
ADMIN_PASSWORD=replace-admin-password
MANAGER_PASSWORD=replace-manager-password
STAFF_PASSWORD=replace-staff-password

# The local demo uses data/cms.json so it runs without external services.
# Use database/mysql-schema.sql as the production schema when wiring MySQL.
DATABASE_URL=mysql://user:password@localhost:3306/aura_wellness
